{{map-tools/base
  class=(concat flexberryClassNames.wrapper " " class)
  name=_identifyToolName
  caption=caption
  tooltip=tooltip
  iconClass=iconClass
  leafletMap=leafletMap
  mapToolProperties=(hash
    toolMode=toolMode
    layerMode=layerMode
    bufferActive=bufferActive
    bufferUnits=bufferUnits
    bufferRadius=bufferRadius
    layers=layers
  )
}}