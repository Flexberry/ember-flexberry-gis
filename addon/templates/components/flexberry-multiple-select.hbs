<select class="ui fluid search dropdown" multiple="" id={{selectorName}}>
  <option value="">{{heading}}</option>
  {{#each items as |item|}}
    <option value="{{item}}">{{item}}</option>
  {{/each}}
</select>
