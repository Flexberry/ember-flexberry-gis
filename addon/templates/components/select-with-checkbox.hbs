<label>{{title}}</label>
<div class="{{selectorName}} ">
  <input name="{{selectorName}}" type="hidden" value={{selectedItems}}> 
  <div class="multiselect">
    <div class="selectBox" >
      <label class="countChoose">{{t 'components.flexberry-layers-intersections-panel.chooseCaption'}}: {{countChoose}}</label>
      <input type="text" class="search" onkeyup={{action 'autoComplete'}}/>	
      <i class="dropdown icon" onclick={{action 'showCheckboxes'}}></i>	  
      <i class="close icon" {{action 'clear'}}></i>
    </div>
    <div class="checkboxes" onchange={{action 'changeCheckbox'}} height={{heightDiv}}>
      {{#if isObject}}
      {{#each items as |item|}}
        <label><input type="checkbox" value="{{item.id}}" />{{item.name}}</label>
      {{/each}}
      {{else}}
      {{#each items as |item|}}
        <label><input type="checkbox" value="{{item}}" />{{item}}</label>
      {{/each}}
      {{/if}}
      <p class="hidden">{{t 'components.flexberry-layers-intersections-panel.notResult'}}</p>
    </div>
  </div>
</div>