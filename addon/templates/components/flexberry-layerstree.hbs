{{#if hasBlock}}
  {{#flexberry-tree
    class=class
    exclusive=exclusive
    collapsible=collapsible
    animateChildren=animateChildren
    duration=duration
    nodes=nodes
  }}
    {{yield}}
  {{/flexberry-tree}}
{{else}}
  {{flexberry-tree
    class=class
    exclusive=exclusive
    collapsible=collapsible
    animateChildren=animateChildren
    duration=duration
    nodes=nodes
  }}
{{/if}}
