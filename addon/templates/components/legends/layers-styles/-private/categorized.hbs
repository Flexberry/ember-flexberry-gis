{{#each styleSettings.style.categories as |category|}}
  {{legends/layers-styles/simple
    styleSettings=category.styleSettings
    legendSettings=legendSettings
    showCaption=true
    caption=category.name
  }}
{{/each}}