{{#if _hasNodes}}
  {{#each (get this _treeNodesPropertyName) as |nodeProperties|}}
    {{component _treeNodeComponentName dynamicProperties=nodeProperties}}
  {{/each}}
{{/if}}
{{#if hasBlock}}
  {{yield}}
{{/if}}
