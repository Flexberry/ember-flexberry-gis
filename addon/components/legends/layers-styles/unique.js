import CategorizedLayerStyleLegendComponent from './-private/categorized';

/**
  Component representing legend for vector layer with 'unique' style.

  @class UniqueLayerStyleLegendComponent
  @extends CategorizedLayerStyleLegendComponent
*/
export default CategorizedLayerStyleLegendComponent.extend({
});
