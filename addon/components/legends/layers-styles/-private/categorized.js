import BaseLayerStyleLegendComponent from './base';
import layout from '../../../../templates/components/legends/layers-styles/-private/categorized';

/**
  Base component representing legend for vector layer with 'categorized' style.

  @class CategorizedLayerStyleLegendComponent
  @extends BaseLayerStyleLegendComponent
*/
export default BaseLayerStyleLegendComponent.extend({
  /**
    Reference to component's template.
  */
  layout
});
