import { isNone } from 'dummy/helpers/is-none';
import { module, test } from 'qunit';

module('Unit | Helper | is none');

// Replace this with your real tests.
test('it works', function(assert) {
  let result = isNone([42]);
  assert.ok(result);
});
