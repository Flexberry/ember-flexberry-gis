import { isOpenLayer } from 'dummy/helpers/is-open-layer';
import { module, test } from 'qunit';

module('Unit | Helper | is open layer');

// Replace this with your real tests.
test('it works', function(assert) {
  let result = isOpenLayer([42]);
  assert.ok(result);
});
