import { getField } from 'dummy/helpers/get-field';
import { module, test } from 'qunit';

module('Unit | Helper | get field');

// Replace this with your real tests.
test('it works', function(assert) {
  let result = getField([42]);
  assert.ok(result);
});
