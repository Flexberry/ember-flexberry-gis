import { flexberrySearchPropertiesOsmRu } from 'dummy/helpers/flexberry-search-properties-osm-ru';
import { module, test } from 'qunit';

module('Unit | Helper | flexberry search properties osm ru');

// Replace this with your real tests.
test('it works', function(assert) {
  let result = flexberrySearchPropertiesOsmRu([42]);
  assert.ok(result);
});
