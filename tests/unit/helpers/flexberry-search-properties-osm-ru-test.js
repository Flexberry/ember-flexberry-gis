import { flexberrySearchPropertiesOsmRu } from 'dummy/helpers/flexberry-search-properties-osm-ru';
import { module, test } from 'qunit';

module('Unit | Helper | flexberry search properties osm ru', function () {
  // Replace this with your real tests.
  test('it works', function (assert) {
    const result = flexberrySearchPropertiesOsmRu([42]);
    assert.ok(result);
  });
});
