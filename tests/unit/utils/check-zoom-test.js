import checkZoom from 'dummy/utils/check-zoom';
import { module, test } from 'qunit';

module('Unit | Utility | check zoom');

// Replace this with your real tests.
test('it works', function(assert) {
  let result = checkZoom();
  assert.ok(result);
});
